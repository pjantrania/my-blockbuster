{% extends "base" %}

{% block content %}
  <div class="row">
	<div class="col-sm-4">
	  <img src="{{m.movie.poster_uri}}" />
	</div>
	<div class="col">
	  <h1>{{m.movie.title}}</h1>
	</div>
  </div>
{% endblock content %}
